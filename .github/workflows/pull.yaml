name : pull pipeline
on:
     pull_request:
       branches : ["main"]
jobs:
     demo:
      runs-on : [self-hosted,Linux]
      steps:
        - name : demo pull check
          run : echo "test sample run"
